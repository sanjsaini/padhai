# padhai
To run: step to run the project
1. padh-ai
- navigate to the project folder in terminal(mac) or cmd(window)
- npm install (only once when do setup)
- npm run start (everytime when you run the frontend)



2. backend
- navigate to the project folder I terminal or cdm
- npm install (only once)
- npm run dev (every time to start project)
